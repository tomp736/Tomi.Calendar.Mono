@page "/note/{id}"
@page "/note/new"

<Tomi.Calendar.Mono.Client.Components.Notes.NoteEditView Id="ViewId" />

@code
{
    [Parameter]
    public string Id { get; set; }

    public Guid ViewId { get; set; }

    protected override void OnInitialized()
    {
        ViewId = Id != null ? Guid.Parse(Id) : Guid.NewGuid();
    }

    protected override void OnParametersSet()
    {
        ViewId = Id != null ? Guid.Parse(Id) : Guid.NewGuid();
    }
}