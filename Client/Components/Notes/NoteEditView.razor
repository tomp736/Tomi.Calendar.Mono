@if (this.Note != null)
{
    <div>
        <div class="form-group">
            <label for="tag-name">Title</label>
            <input class="form-control" type="text" id="tag-name" @bind="Note.Title" />
        </div>
        <TextEditor @ref="NoteTextEditor" HTMLContent="@Note.Content" ReadOnly="@ReadOnly"></TextEditor>
        <button class="btn btn-primary" @onclick="SaveItem">Ok</button>
        @if (!IsNew)
        {
            <button class="btn btn-danger" @onclick="DeleteItem">Delete</button>
        }
        @if (ModalInstance != null)
        {
            <button class="btn btn-secondary" @onclick="ModalInstance.CancelAsync">Cancel</button>
        }
    </div>
}
